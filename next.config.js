// next.config.js
module.exports = {
  output: 'export',  // Enables static export (if you're using next export)
  images: {
    unoptimized: true,  // Optional: if you're handling image optimization yourself
  },
}
